# Анализ проекта Server Manager

## Описание проекта
Telegram-бот для управления Linux-сервером через systemd сервисы. Позволяет мониторить систему, управлять сервисами, просматривать статистику и выполнять административные задачи.

## Структура проекта
```
server_manager/
├── main.py                  # Точка входа приложения
├── handlers_async.py        # Обработчики Telegram команд и callback'ов
├── system_controller.py     # Класс для управления системой
├── cpu_count.py            # Утилита для работы с CPU (проблемный файл)
├── df.py                   # Утилита для проверки диска
└── .env                    # Конфигурация (не в репозитории)
```

---

## КРИТИЧЕСКИЕ ПРОБЛЕМЫ БЕЗОПАСНОСТИ

### 🔴 1. Выполнение произвольных команд (CRITICAL)
**Файл:** `system_controller.py:249-255`, `handlers_async.py:413-421`

**Проблема:**
```python
async def run_command(self, command):
    """ Запускает команду в терминале """
    try:
        output = subprocess.check_output(command, stderr=subprocess.STDOUT).decode()
        return output
    except subprocess.CalledProcessError as e:
        return e.output.decode()
```

Пользователь может выполнить **ЛЮБУЮ** команду через `///run_command`, включая:
- Удаление файлов: `///run_command rm -rf /`
- Кража данных: `///run_command cat /etc/passwd`
- Изменение системных файлов
- Установка backdoor'ов

**Уровень опасности:** 🔴 КРИТИЧЕСКИЙ
**Рекомендации:**
1. УДАЛИТЬ эту функцию полностью
2. Если необходима - создать whitelist разрешенных команд
3. Никогда не передавать пользовательский ввод напрямую в shell

---

### 🔴 2. Command Injection через shell команды
**Файл:** `system_controller.py:107-121`, `system_controller.py:249-255`

**Проблема:**
```python
async def find_and_kill(self, pattern):
    subprocess.run(['pgrep', '-f', pattern], stdout=subprocess.PIPE, text=True)
    subprocess.run(['kill', pid])
```

Хотя `pgrep -f` безопаснее, пользователь может передать шаблон, который убьет критические процессы:
- `///kill systemd` - убьет init процесс
- `///kill .` - попытается убить все процессы

**Уровень опасности:** 🔴 ВЫСОКИЙ
**Рекомендации:**
1. Создать whitelist разрешенных процессов для убийства
2. Добавить подтверждение перед убийством процесса
3. Запретить убивать системные процессы (PID < 1000)

---

### 🟠 3. Hardcoded абсолютные пути
**Файл:** `handlers_async.py:480`

**Проблема:**
```python
with open("/root/myservermanager/not_allowed.txt", "a") as f:
```

**Проблемы:**
- Работает только на конкретной системе
- Раскрывает структуру директорий
- Может не иметь прав записи

**Рекомендации:**
- Использовать относительные пути или конфигурацию
- Добавить обработку ошибок при записи

---

### 🟠 4. Отсутствие rate limiting
**Файлы:** Все обработчики

**Проблема:**
Пользователь может заспамить бота запросами:
- `///загрузка` - выполняет `ps` 50 раз
- Множественные перезагрузки
- DoS атака на бота

**Рекомендации:**
1. Добавить rate limiting (например, aiogram middleware)
2. Ограничить частоту выполнения тяжелых команд
3. Добавить очередь запросов

---

### 🟠 5. Логирование попыток доступа в plaintext
**Файл:** `handlers_async.py:480-481`

**Проблема:**
```python
f.write(f"{message.from_user.id}: {message.text}\n")
```

- Сохраняет ID и текст сообщений без шифрования
- Может нарушать GDPR/защиту персональных данных
- Файл доступен всем с правами чтения

**Рекомендации:**
1. Использовать proper logging библиотеку
2. Хешировать user_id
3. Настроить ротацию логов

---

### 🟠 6. Sudo без дополнительных проверок
**Файл:** `system_controller.py` (множество мест)

**Проблема:**
```python
subprocess.run(["sudo", "systemctl", "restart", service_name], check=True)
subprocess.run(["sudo", "reboot"], check=True)
```

- Предполагается, что у пользователя есть NOPASSWD sudo
- Нет проверки успешности выполнения sudo команд
- Нет логирования sudo операций

**Рекомендации:**
1. Настроить sudoers файл корректно
2. Логировать все sudo операции
3. Добавить timeout для sudo команд

---

## ПРОБЛЕМЫ АРХИТЕКТУРЫ

### 🟡 7. cpu_count.py выполняет код при импорте
**Файл:** `cpu_count.py:11-34`

**Проблема:**
```python
pid = os.getpid()
set_affinity(pid, [1])  # ❌ Выполняется при импорте!
print(pid)
print(1)
cpu_count = get_cpu_count()
print(f"Общее количество ядер CPU: {cpu_count}")
```

**Последствия:**
- Файл нельзя безопасно импортировать
- Устанавливает CPU affinity при каждом импорте
- Печатает в консоль при импорте
- Вызывает ошибку из-за неправильных аргументов

**Рекомендации:**
1. Обернуть код в `if __name__ == '__main__':`
2. Создать функции вместо выполнения кода в глобальной области

---

### 🟡 8. Дублирование кода
**Файлы:** `main.py:26-30`, `cpu_count.py:5-9`

**Проблема:**
Функция `set_affinity` определена дважды с разными сигнатурами:
```python
# main.py
def set_affinity(cores):
    pid = os.getpid()
    os.sched_setaffinity(pid, cores)

# cpu_count.py
def set_affinity(cores):  # Та же функция!
    pid = os.getpid()
    os.sched_setaffinity(pid, cores)
```

**Рекомендации:**
- Создать модуль `utils.py` для общих функций
- Использовать одну реализацию

---

### 🟡 9. Создание SystemController в каждом обработчике
**Файл:** `handlers_async.py:182, 370`

**Проблема:**
```python
async def callback_query_handler(query):
    controller = SystemController()  # ❌ Новый экземпляр!

async def gpt_chat(message):
    controller = SystemController()  # ❌ Еще один!
```

**Последствия:**
- Неэффективное использование памяти
- Каждый раз читается .env файл
- Нет переиспользования ресурсов

**Рекомендации:**
1. Создать один глобальный экземпляр
2. Или использовать dependency injection
3. Кэшировать конфигурацию

---

### 🟡 10. Смешивание ответственности в handlers_async.py
**Файл:** `handlers_async.py`

**Проблема:**
Один файл содержит:
- Keyboard layouts (строки 23-156)
- Handlers (строки 161-488)
- Helper functions (строки 335-352)
- Business logic (встроена в handlers)

**Рекомендации:**
Разделить на модули:
```
keyboards/
  main_menu.py
  service_menu.py
  stats_menu.py
handlers/
  service_handler.py
  stats_handler.py
  system_handler.py
utils/
  formatters.py
```

---

### 🟡 11. Отсутствие обработки ошибок
**Файл:** `system_controller.py` (множество мест)

**Проблема:**
```python
async def restart_service_by_name(self, service_name):
    subprocess.run(["sudo", "systemctl", "restart", service_name], check=True)  # ❌ Может упасть
```

**Последствия:**
- Бот крашится при ошибке systemctl
- Пользователь не получает информацию об ошибке
- Нет логирования проблем

**Рекомендации:**
```python
async def restart_service_by_name(self, service_name):
    try:
        result = subprocess.run(
            ["sudo", "systemctl", "restart", service_name],
            check=True,
            capture_output=True,
            text=True,
            timeout=30
        )
        return True, "Сервис перезапущен"
    except subprocess.TimeoutExpired:
        return False, "Превышен timeout перезапуска"
    except subprocess.CalledProcessError as e:
        return False, f"Ошибка: {e.stderr}"
```

---

## ЛОГИЧЕСКИЕ ОШИБКИ

### 🔵 12. Неправильный вызов set_affinity
**Файл:** `cpu_count.py:12`

**Проблема:**
```python
def set_affinity(cores):
    pid = os.getpid()
    os.sched_setaffinity(pid, cores)

pid = os.getpid()
set_affinity(pid, [1])  # ❌ Передается PID вместо cores!
```

**Ожидалось:**
```python
set_affinity([1])
```

---

### 🔵 13. Двойной импорт os
**Файл:** `cpu_count.py:1,3`

```python
import os
import multiprocessing
import os  # ❌ Дубликат
```

---

### 🔵 14. Несоответствие типов в проверке пользователей
**Файл:** `handlers_async.py:168, 485`

**Проблема:**
```python
# handlers_async.py:168
if str(message.from_user.id) not in ALLOWED_USERS:  # ✅ Правильно

# handlers_async.py:485
if message.from_user.id not in ALLOWED_USERS:  # ❌ Неправильно! int vs str
```

**Последствия:**
- Функция `voice_to_text` пропустит всех пользователей (проверка всегда True)

**Исправление:**
```python
if str(message.from_user.id) not in ALLOWED_USERS:
```

---

### 🔵 15. Неправильное использование subprocess.check_output
**Файл:** `system_controller.py:252`

**Проблема:**
```python
async def run_command(self, command):
    output = subprocess.check_output(command, stderr=subprocess.STDOUT).decode()
```

- Если `command` это строка типа "ls -la", нужен `shell=True`
- Без `shell=True` будет искать бинарник с именем "ls -la"

**Рекомендации:**
```python
# Вариант 1: Использовать список
subprocess.check_output(['ls', '-la'])

# Вариант 2: Shell (ОПАСНО!)
subprocess.check_output(command, shell=True)  # ❌ Command injection!
```

---

### 🔵 16. Функция crone вместо cron
**Файл:** `handlers_async.py:39, 271-283`

**Проблема:**
```python
InlineKeyboardButton("Crone", callback_data="crone")  # ❌ Опечатка
```

Название "crone" (старуха) вместо "cron" (планировщик задач)

---

## ПРОБЛЕМЫ КАЧЕСТВА КОДА

### 🟢 17. Большое количество закомментированного кода
**Файлы:** Множество

**Примеры:**
```python
# handlers_async.py:4-21
# class BotStates(StatesGroup):
#     home = State()
#     ...

# handlers_async.py:46
# dp.register_message_handler(service_management_handler, state=BotStates.service_management)

# system_controller.py:174-189
# # Получение PID'ов процессов, связанных с сервисом
# # pids = subprocess.check_output(...)
```

**Рекомендации:**
- Удалить весь закомментированный код
- Использовать git для истории изменений

---

### 🟢 18. Отсутствие логирования
**Файлы:** Все

**Проблема:**
Только несколько `print()` вместо proper logging:
```python
# system_controller.py:203
print(er)

# cpu_count.py:30
print(pid)
print(1)
```

**Рекомендации:**
```python
import logging

logger = logging.getLogger(__name__)

# Вместо print
logger.error(f"Service error: {er}")
logger.info(f"Process PID: {pid}")
```

---

### 🟢 19. Отсутствие документации
**Проблемы:**
- ❌ Нет README.md
- ❌ Нет requirements.txt (gitignore блокирует *.txt!)
- ❌ Нет инструкции по установке
- ❌ Нет описания переменных окружения

**Минимально необходимо:**
```markdown
# README.md
## Установка
## Конфигурация (.env)
## Запуск
## Доступные команды
```

---

### 🟢 20. Проблемы с .gitignore
**Файл:** `.gitignore:5`

**Проблема:**
```gitignore
*.txt  # ❌ Блокирует requirements.txt!
```

**Рекомендации:**
```gitignore
# Вместо *.txt указать конкретные файлы
not_allowed.txt
logs/*.txt

# Но НЕ игнорировать
!requirements.txt
```

---

### 🟢 21. Hardcoded значения
**Файл:** `main.py:31`

```python
set_affinity([1])  # ❌ Почему ядро 1? Почему hardcoded?
```

**Рекомендации:**
- Вынести в конфигурацию
- Добавить комментарий, почему именно это ядро

---

### 🟢 22. Неиспользуемые импорты и переменные
**Файлы:** Множество

```python
# main.py:14
from aiogram import Bot, Dispatcher #, types  # Закомментированный импорт

# main.py:16-17
#from aiogram.types import ParseMode
#from aiogram.utils import executor
```

---

### 🟢 23. Магические числа
**Файлы:** Множество

```python
# system_controller.py:96
output.strip()[:400]  # ❌ Почему 400?

# system_controller.py:127
async def top_cpu(self, n=50):  # ❌ Почему 50?

# system_controller.py:149
async def top_mem_processes(self, n=50):  # ❌ Почему 50?
```

**Рекомендации:**
```python
MAX_INFO_LENGTH = 400
DEFAULT_TOP_PROCESSES = 50
```

---

### 🟢 24. Неконсистентное форматирование строк
**Файлы:** Множество

```python
# Смесь разных стилей:
f"Информация о сервисе {service_name}:\n{output.strip()[:400]}"  # f-string
"///перезагрузить - перезагрузить компьютер\n" + ...  # Конкатенация
```

**Рекомендации:**
- Использовать f-strings везде
- Или использовать .format() везде

---

## ПРОБЛЕМЫ ПРОИЗВОДИТЕЛЬНОСТИ

### 🟣 25. Синхронные вызовы subprocess в async функциях
**Файлы:** `system_controller.py` (все методы)

**Проблема:**
```python
async def restart_service_by_name(self, service_name):
    subprocess.run(["sudo", "systemctl", "restart", service_name], check=True)  # ❌ Блокирующий вызов!
```

**Последствия:**
- Блокирует event loop
- Другие пользователи ждут
- Бот может зависнуть

**Рекомендации:**
```python
async def restart_service_by_name(self, service_name):
    process = await asyncio.create_subprocess_exec(
        "sudo", "systemctl", "restart", service_name,
        stdout=asyncio.subprocess.PIPE,
        stderr=asyncio.subprocess.PIPE
    )
    stdout, stderr = await process.communicate()
    return process.returncode == 0
```

---

### 🟣 26. Неэффективная работа с процессами
**Файл:** `system_controller.py:161`

**Проблема:**
```python
process_name = parts[-1][:14] + "|" + parts[-1][-15:]  # ❌ Зачем обрезать?
```

Неочевидная логика обрезания имени процесса

---

## ОТСУТСТВУЮЩИЕ КОМПОНЕНТЫ

### 27. Нет unit-тестов
- ❌ Нет pytest
- ❌ Нет тестов для критической логики
- ❌ Нет CI/CD

### 28. Нет мониторинга
- ❌ Нет healthcheck endpoint'а
- ❌ Нет метрик
- ❌ Нет alerting при падении

### 29. Нет graceful shutdown
**Файл:** `main.py:49-52`

```python
try:
    await dp.start_polling()
finally:
    await bot.close()  # ✅ Хорошо, но недостаточно
```

**Нужно добавить:**
- Обработку SIGTERM/SIGINT
- Завершение текущих операций
- Сохранение состояния

---

## РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТАМ

### 🔴 КРИТИЧНО (исправить немедленно):
1. ✅ Удалить функцию `run_command` или создать whitelist
2. ✅ Исправить проверку пользователей в `voice_to_text`
3. ✅ Добавить обработку ошибок в SystemController
4. ✅ Исправить `cpu_count.py` (код при импорте)

### 🟠 ВЫСОКИЙ ПРИОРИТЕТ (исправить на этой неделе):
5. ✅ Добавить rate limiting
6. ✅ Создать whitelist для `find_and_kill`
7. ✅ Заменить синхронные subprocess на async
8. ✅ Настроить proper logging

### 🟡 СРЕДНИЙ ПРИОРИТЕТ (исправить в этом месяце):
9. ✅ Рефакторинг: разделить handlers_async.py на модули
10. ✅ Создать singleton SystemController
11. ✅ Добавить requirements.txt
12. ✅ Написать README.md
13. ✅ Удалить закомментированный код

### 🟢 НИЗКИЙ ПРИОРИТЕТ (backlog):
14. ✅ Добавить unit-тесты
15. ✅ Настроить CI/CD
16. ✅ Добавить мониторинг
17. ✅ Улучшить документацию кода

---

## ИТОГИ

### Статистика проблем:
- 🔴 Критические проблемы безопасности: **6**
- 🟡 Архитектурные проблемы: **5**
- 🔵 Логические ошибки: **5**
- 🟢 Проблемы качества кода: **8**
- 🟣 Проблемы производительности: **2**
- Отсутствующие компоненты: **3**

**ВСЕГО: 29 проблем**

### Общая оценка проекта:
- **Функциональность:** ✅ Работает
- **Безопасность:** 🔴 КРИТИЧЕСКИЕ уязвимости
- **Архитектура:** 🟡 Требует рефакторинга
- **Качество кода:** 🟡 Ниже среднего
- **Производительность:** 🟣 Проблемы с async
- **Документация:** ❌ Отсутствует

### Рекомендация:
**НЕ использовать в production без исправления критических проблем безопасности!**

Проект требует серьезного рефакторинга, особенно в части:
1. Безопасности (command injection, arbitrary command execution)
2. Асинхронности (блокирующие вызовы)
3. Архитектуры (разделение ответственности)
4. Обработки ошибок

---

**Дата анализа:** 2025-11-14
**Анализатор:** Claude Code
**Версия проекта:** commit 1e95221
